namespace Thunderbirds.Rules
{
    /// <summary>
    /// Placeholder so the Rules assembly compiles before any real rules exist.
    /// Delete once the first rules-layer type lands (issue #3).
    /// </summary>
    public static class RulesAssemblyMarker
    {
        public const string Name = "Thunderbirds.Rules";
    }
}
